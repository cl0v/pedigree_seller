name: pedigree_seller
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  firebase_core: "^1.1.0"
  cloud_firestore: "^2.2.0"
  firebase_auth: "^1.2.0"
  shared_preferences: ^2.0.5
  firebase_storage: ^8.1.0
  file_picker: ^3.0.2+2
  dio: ^4.0.0

  commons:
    path: ../../commons

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
